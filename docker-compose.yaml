services:
  backend:
    depends_on:
      postgres:
        condition: service_healthy
    build:
      context: ./backend
      dockerfile: Dockerfile
      target: backend
    ports:
      - "8090:8090"
    environment:
      - CONFIG_PATH=./config/local.yaml
    networks:
      - hackmate-network

  postgres:
    image: postgres:17
    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=hackmate
    volumes:
      - postgres:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U user -d scheduler"]
      interval: 3s
      timeout: 2s
      retries: 2
    networks:
      - hackmate-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:80"  # Nginx слушает на 80 внутри контейнера
    depends_on:
      - backend
    environment:
      - VITE_API_URL=/api  # Используем прокси через nginx
    networks:
      - hackmate-network

volumes:
  postgres:

networks:
  hackmate-network:
    driver: bridge